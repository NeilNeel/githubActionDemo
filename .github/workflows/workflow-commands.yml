name: Workflow Commands Demo
on: workflow_dispatch
jobs:
  workflow-cmd-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Group logs
        run: |
          echo "::group::My Group Logs"
          echo "Msg 1"
          echo "Msg 2"
          echo "::endgroup::"
